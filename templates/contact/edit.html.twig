{% extends 'base.html.twig' %}

{% block title %}Editer Contact{% endblock %}

{% block body %}
    <h3>Editer Contact</h3>

    {{ include('contact/_form.html.twig', {'button_label': 'Update'}) }} ||

    <a href="{{ path('contact_index') }}">retour à la liste</a>|| {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
