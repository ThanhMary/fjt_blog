{% extends 'base.html.twig' %}

{% block title %}Edit Contact{% endblock %}

{% block body %}
    <h3>Edit Contact</h3>

    {{ include('contact/_form.html.twig', {'button_label': 'Update'}) }} ||

    <a href="{{ path('contact_index') }}">back to list</a>|| {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
